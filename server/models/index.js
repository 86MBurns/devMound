const User = require('./User');
const Employer = require('./Employer');
const Jobs = require('./Jobs');
const Application = require('./Application');

module.exports = { User, Employer, Jobs, Application};